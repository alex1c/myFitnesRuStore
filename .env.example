# Мой спортзал — environment placeholders only.
# Copy to `.env` locally if needed. Never commit real secrets.

# Example (unused in Phase 0 — app is fully offline):
# EXPO_PUBLIC_APP_NAME=Мой спортзал
